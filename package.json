{
  "name": "all-animal-r-r",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "react-dotenv && node server/server.js",
    "develop": "react-dotenv && concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "seed": "cd server && npm run seed",
    "build": "react-dotenv && cd client && npm run build"
  },
  "keywords": [],
  "author": "Mary Elenius",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^5.1.0"
  },
  "dependencies": {
    "react-dotenv": "^0.1.3"
  }
}
